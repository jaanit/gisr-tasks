import { Box } from "@mantine/core";

export default function SmallLogo() {
  return (
    <Box bg={"blue"} className="rounded-md">
      <span className={`text-xl  font-bold text-white p-2 w-[80px] h-[40px] flex justify-center items-center`}>G</span>
    </Box>
  );
}
