import { Box } from "@mantine/core";

export default function Logo() {
  return (
    <Box bg={"blue"} className="rounded-md">
      <span className={`text-3xl font-bold text-white w-[50px] h-[50px] flex justify-center items-center`}>G</span>
    </Box>
  );
}
